# Frontend — PrimeHomes Real Estate Lead Bot

Modern React (Vite) customer chat + sales leads view.

**Theme:** Orange & yellow

```bash
cd frontend
npm install
npm run dev
```

- `/` — Customer chat
- `/leads` — Sales lead list
