# Tests — Real Estate Lead Bot

```
tests/
├── backend/    # API, services, models, qualification
├── frontend/   # Component / page tests (later)
└── e2e/        # End-to-end journeys
```

Backend unit/integration tests also live under `backend/tests/` for colocation with the application code.

Testing strategy is defined in `docs/TESTING_SPEC.md`.
